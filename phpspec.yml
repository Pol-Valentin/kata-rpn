suites:
    default:
        namespace: RPN
        spec_path: %paths.config%/tests/
        psr4_prefix: RPN

extensions:
#    - PhpSpec\Extension\CodeCoverageExtension

code_coverage:
    format:
        - html
    output:
        html: tests/output/

